# Build the Docker Image
docker build -t my-app .

# Authenticate Docker to Amazon ECR
aws ecr get-login-password --region <region> | docker login --username AWS --password-stdin <account-id>.dkr.ecr.<region>.amazonaws.com

# Tag the Docker Image with the ECR Repository URL
docker tag my-app:latest <account-id>.dkr.ecr.<region>.amazonaws.com/<repository-name>:latest

# Push the Docker Image to ECR
docker push <account-id>.dkr.ecr.<region>.amazonaws.com/<repository-name>:latest



# Build the Docker Image
docker build -t de-presigned-url-lambda-image .

# Authenticate Docker to Amazon ECR
aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 833695691939.dkr.ecr.us-east-1.amazonaws.com

# Tag the Docker Image with the ECR Repository URL
docker tag de-presigned-url-lambda-image:latest 833695691939.dkr.ecr.us-east-1.amazonaws.com/de_presigned_url_ecr:latest

# Push the Docker Image to ECR
docker push 833695691939.dkr.ecr.us-east-1.amazonaws.com/de_presigned_url_ecr:latest
